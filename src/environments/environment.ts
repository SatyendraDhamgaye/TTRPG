// Default environment used as a fallback.
// Specific build configurations replace this file.
export const environment = {
  name: 'default',
  production: false,
  apiBaseUrl: 'http://localhost:4200',
  assetsBaseUrl: '/'
};